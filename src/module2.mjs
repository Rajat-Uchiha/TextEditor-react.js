const name = "Rajat";
const age = 20;
const language = "Javascript"

export default name; //Here by default the value of name will be exported.If I will write age then the value of age will be exported and if I will write language then javascript will be exported

export { age }; //This is the way by which we can export module other than default method.
export { language };  //This is the way by which we can export module other than default method.