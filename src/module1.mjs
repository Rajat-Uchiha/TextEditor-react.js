import nm, { age, language } from "./module2.mjs"
// The module exported by default method can be import with normal variable but the module other than default method are import within the  curly braces

console.log(nm); //Here the value of nm will be displayed that is =>Rajat
console.log(age);
console.log(language);
